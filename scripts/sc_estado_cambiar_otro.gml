///sc_estado_cambiar_otro(obj, estado)

/*

    @Desc:
        Se cambia el estado de la instancia o instancias recibidas en el estado indicado. Debe cumpliar que: tener atributo "mi_estado" y que
       el estado recibido exista.
       
    @Return: nada. 

*/

with( argument0 ){ mi_estado = argument1; }
